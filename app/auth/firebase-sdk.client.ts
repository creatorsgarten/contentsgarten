import { initializeApp } from 'firebase/app'
import {
  getAuth,
  onAuthStateChanged,
  onIdTokenChanged,
  signInWithPopup,
  signOut,
  GithubAuthProvider,
} from 'firebase/auth'

export {
  initializeApp,
  getAuth,
  onAuthStateChanged,
  onIdTokenChanged,
  signInWithPopup,
  signOut,
  GithubAuthProvider,
}
